import 'package:client/Api.dart';


    // Map<String, dynamic> children = {};
    // if (widget.noteData.containsKey('children')) {
    //   for (var child in widget.noteData['children']) {
    //     getNote(child).then((value) => {widget.children[child] = value});
    //   }
    // }